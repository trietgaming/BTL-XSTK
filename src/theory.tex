% !TEX root = main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Cơ sở Lý thuyết}
\label{sec:co_so_ly_thuyet}

\subsection{Định giá GPU: Hiệu năng, thương hiệu và thời gian}

Card đồ họa (GPU) là một trong số ít linh kiện điện tử mà giá bán và thông số kỹ thuật thường không tỷ lệ tuyến tính với nhau. Một GPU flagship của NVIDIA có thể đắt gấp đôi một card AMD cùng thế hệ dù chênh lệch hiệu năng không tương xứng --- phần còn lại là \textit{định giá thương hiệu}, \textit{chiến lược phân khúc} và \textit{hệ sinh thái phần mềm}. Điều đó đặt ra câu hỏi không đơn giản: \textbf{giá phát hành GPU thực sự được cấu thành bởi những yếu tố nào?}

Đề tài tiếp cận câu hỏi này qua hai góc nhìn bổ trợ:

\begin{itemize}
  \item \textbf{Hồi quy đa biến}: Dự đoán giá và định lượng tác động đồng thời của TDP, xung nhịp, dung lượng bộ nhớ, năm phát hành và thương hiệu lên giá. Mô hình cho phép trả lời: nếu tăng VRAM thêm 1 độ lệch chuẩn trong khi giữ nguyên mọi thông số khác, giá GPU thay đổi bao nhiêu phần trăm?
  \item \textbf{Kiểm định ANOVA}: Kiểm tra riêng biệt xem khoảng cách giá NVIDIA--AMD có ý nghĩa thống kê, trước khi đưa biến thương hiệu vào mô hình đa biến như một biến giả.
\end{itemize}

Thách thức nằm ở chỗ phần lớn GPU trong bộ dữ liệu không có giá niêm yết chính thức ($\approx 84\%$ thiếu giá), và 34 thuộc tính ban đầu chứa nhiều thông tin dư thừa. Các phần tiếp theo mô tả phương pháp luận để giải quyết những thách thức đó.

\subsection{Bản chất dữ liệu} \label{sec:data_characteristics}

Dữ liệu phần cứng GPU mang một số đặc thù quan trọng cần lưu ý trước khi tiến hành xử lý và xây dựng mô hình:

\begin{itemize}
  \item \textbf{Đặc tính rời rạc của bộ nhớ:} Các biến số như băng thông bộ nhớ (\texttt{Memory\_Bus}) và dung lượng bộ nhớ (\texttt{Memory}) mang bản chất là các biến \textbf{rời rạc}. Chúng chỉ tồn tại theo các chuẩn phần cứng nhất định (ví dụ: bus 64/128/256/512 bit; dung lượng 2/4/8/16~GB).
  \item \textbf{Sự phân mảnh nhãn thương hiệu (ATI và AMD):} ATI Technologies là tên cũ của bộ phận GPU của AMD trước khi bị thâu tóm vào năm 2006. Hiện tại, bộ dữ liệu thô tồn tại song song hai nhãn \texttt{"ATI"} và \texttt{"AMD"} cho cùng một dòng sản phẩm. Hai nhãn này thực chất cần được gộp chung thành một thực thể thương hiệu duy nhất.
  \item \textbf{Sự khuyết thiếu dữ liệu giá của Intel:} Toàn bộ các GPU của hãng Intel trong bộ dữ liệu thô đều thiếu thông tin về biến phụ thuộc là giá phát hành (\texttt{Release\_Price}), do đó các sản phẩm này không thể được sử dụng trong việc xây dựng mô hình định giá và cần loại bỏ hoàn toàn.
  \item \textbf{Các sản phẩm có cấu trúc định giá dị biệt:} Dữ liệu chứa một số nhóm sản phẩm có cơ chế định giá nằm ngoài quy luật phần cứng thông thường và không phụ thuộc vào các thuộc tính có trong bộ dữ liệu. Điển hình như: GPU Workstation (Quadro/FirePro) định giá theo chứng chỉ phần mềm; GPU Titans bị thổi giá do sản xuất limited; các cấu hình kép (Crossfire/Dual Core) thực chất là nhiều GPU bị cộng dồn thông số nhưng bán chung với nhau; các bản độ tản nhiệt nước cao cấp (Hydro Copper); hoặc sản phẩm tin đồn chưa từng ra mắt (not released). Nếu không được thanh lọc cẩn thận, nhóm dữ liệu dị biệt này sẽ gây nhiễu nặng nề cho phương trình dự báo của thị trường Consumer.
\end{itemize}

\subsection{Tiền xử lý dữ liệu}

\subsubsection{Lọc và làm sạch dữ liệu thô}

Trước khi xử lý ngoại lai hay điền khuyết, bộ dữ liệu thô cần trải qua một chuỗi thao tác làm sạch để đảm bảo tính nhất quán và phù hợp với mục tiêu phân tích:

\begin{enumerate}
  \item Thay thế các chuỗi rỗng, dấu gạch ngang và "N/A" thành giá trị NA thực sự trong R để dễ dàng nhận diện và xử lý sau này.

  \item Hợp nhất toàn bộ giá trị \texttt{"ATI"} trong cột \texttt{Manufacturer} thành \texttt{"AMD"}.

  \item Loại bỏ các dòng có \texttt{Manufacturer == "Intel"}.
  
  \item Tách Năm từ cột Release\_Date, đổi tên thành Release\_Year và xóa cột Release\_Date gốc. Việc này không chỉ chuẩn hóa định dạng ngày tháng mà còn loại bỏ nhiễu từ ngày và tháng không liên quan đến định giá (chu kỳ giá GPU tính theo năm).

  \item \textbf{Trích xuất số và ép kiểu dữ liệu.} Các trường số trong dữ liệu gốc thường đi kèm đơn vị văn bản (như \texttt{"141 Watts"}, \texttt{"8 GB"}). Nhóm sử dụng biểu thức chính quy (Regular Expression) để trích xuất chính xác phần số, sau đó thực hiện ép kiểu \texttt{as.numeric()}. Quy trình này đảm bảo các giá trị không hợp lệ hoặc chuỗi rỗng được chuyển thành \texttt{NA} thực sự, tạo tiền đề cho các bước xử lý ngoại lai và điền khuyết.

  \item Chuyển đổi các biến định danh (\texttt{Manufacturer}, \texttt{Memory\_Type}) sang kiểu Factor. Điều này không chỉ giúp R tự động sinh biến giả (dummy variables) trong mô hình hồi quy mà còn đảm bảo rằng các biến này được xử lý đúng cách như các nhóm phân loại thay vì số liên tục.
\end{enumerate}

\subsubsection{Loại bỏ tất cả dữ liệu thiếu giá}

Nguyên tắc rất quan trọng: Trong mô hình dự đoán, Biến mục tiêu $Y$ (Giá phát hành) là ``vua''. Bất kỳ dòng nào không có $Y$ đều là "rác" đối với quá trình xây dựng mô hình. 
Với lại, \texttt{Release\_Price} có tỷ lệ khuyết rất cao ($\approx 84\%$). Việc điền khuyết sẽ tạo ra vòng lặp logic khi mô hình tự học từ dữ liệu giả do chính nó tạo ra. 

Do đó, nhóm quyết định \textbf{loại bỏ hoàn toàn} các quan sát thiếu giá, giữ lại những mẫu có giá gốc để xây dựng mô hình hồi quy.
\subsection{Thu hẹp không gian đặc trưng}

Đưa toàn bộ 34 thuộc tính vào mô hình không những không cải thiện khả năng dự đoán mà còn làm giảm chất lượng: biến trùng lặp gây đa cộng tuyến, biến không liên quan thêm nhiễu và tăng phương sai ước lượng. Nhóm giải quyết vấn đề này bằng quy trình sàng lọc \textbf{ba tầng khách quan} --- mỗi tầng nhắm đến một nguồn nhiễu hoàn toàn khác nhau:

\begin{figure}[H]
\centering
\resizebox{0.72\textwidth}{!}{
\begin{tikzpicture}[
  tier/.style={rectangle, draw=black!65, fill=#1, rounded corners=4pt,
               text centered, minimum width=8.5cm, minimum height=1.15cm, font=\small},
  note/.style={rectangle, draw=gray!50, fill=gray!5, rounded corners=2pt,
               font=\scriptsize\itshape, text width=3.8cm, align=left,
               inner sep=4pt},
  >=stealth, thick
]
  \node[tier=gray!12] (t0) {Không gian đặc trưng ban đầu: \textbf{34 thuộc tính}};

  \node[tier=yellow!18, below=0.7cm of t0] (t1)
    {\textbf{Tầng 1:} Loại biến không liên quan kinh tế đến giá};
  \node[note, right=0.6cm of t1] {Định danh sản phẩm,\\API phần mềm,\\cờ nhị phân mất cân bằng};

  \node[tier=orange!12, below=0.7cm of t1] (t2)
    {\textbf{Tầng 2:} Loại biến dẫn xuất hoặc thông tin trùng lặp};
  \node[note, right=0.6cm of t2] {Nguy cơ đa cộng\\tuyến (VIF cao)};

  \node[tier=red!10, below=0.7cm of t2] (t3)
    {\textbf{Tầng 3:} Loại biến gây đa cộng tuyến thực nghiệm};
  \node[note, right=0.6cm of t3] {Xác định thực nghiệm\\qua phân tích VIF\\(Phần~\ref{sec:code_xu_ly_du_lieu})};

  \node[tier=green!15, below=0.7cm of t3] (t4)
    {Tập đặc trưng chính thức};

  \draw[->] (t0) -- (t1);
  \draw[->] (t1) -- (t2);
  \draw[->] (t2) -- (t3);
  \draw[->] (t3) -- (t4);
\end{tikzpicture}
}
\caption{Chiến lược lọc ba tầng: thu hẹp không gian đặc trưng.}
\label{fig:filter_strategy}
\end{figure}

Tầng~1 và Tầng~2 có thể xác định hoàn toàn bằng lý luận trước khi nhìn vào dữ liệu. Tầng~3 cần tính giá trị VIF thực tế và được giải quyết trong Phần~\ref{sec:code_xu_ly_du_lieu}.

\renewcommand{\arraystretch}{1.1}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{5.2cm}|p{3.5cm}|p{5.5cm}|}
\caption{Tầng 1 --- 14 biến bị loại do không có ý nghĩa kinh tế}
\label{tab:tier1_vars} \\
\hline
\textbf{Biến bị loại} & \textbf{Nội dung} & \textbf{Lý do cụ thể} \\
\hline
\endfirsthead
\hline
\textbf{Biến bị loại} & \textbf{Nội dung} & \textbf{Lý do cụ thể} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Name}, \texttt{Architecture}
  & Nhãn định danh sản phẩm
  & Không phải đặc tính kỹ thuật định lượng, không có cơ chế trực tiếp tác động đến chi phí sản xuất. \\
\hline
\texttt{DVI\_Connection}, \texttt{HDMI\_Connection}, \texttt{VGA\_Connection}
  & Đặc tả cổng đầu ra hiển thị
  & Số lượng và loại cổng phụ thuộc vào phân khúc sản phẩm, không phản ánh hiệu năng GPU. \\
\hline
\texttt{Direct\_X}, \texttt{Open\_GL}
  & Phiên bản API đồ họa
  & Hoàn toàn phụ thuộc vào driver phần mềm và thế hệ kiến trúc, không phản ánh sức mạnh phần cứng thực. \\
\hline
\texttt{Best\_Resolution}, \texttt{Resolution\_WxH}, \texttt{Power\_Connector}
  & Thông số hiển thị và loại đầu nối nguồn
  & Dẫn xuất gián tiếp từ cấu hình phần cứng, không mang thêm thông tin độc lập về giá trị GPU. \\
\hline
\texttt{Dedicated}, \texttt{Integrated}, \texttt{Notebook\_GPU}, \texttt{SLI\_Crossfire}
  & Số nhị phân mất cân bằng nặng
  & Gần như toàn bộ mẫu thuộc cùng một nhãn (ví dụ: $>$98\% là GPU dedicated), biến không mang thông tin phân biệt. \\
\hline
\end{longtable}
\normalsize

\renewcommand{\arraystretch}{1.1}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{3.5cm}|p{6.8cm}|p{3.2cm}|}
\caption{Tầng 2 --- 10 biến bị loại do thông tin dư thừa (nguy cơ đa cộng tuyến)}
\label{tab:tier2_vars} \\
\hline
\textbf{Biến bị loại} & \textbf{Quan hệ dư thừa} & \textbf{Biến giữ lại liên quan} \\
\hline
\endfirsthead
\hline
\textbf{Biến bị loại} & \textbf{Quan hệ dư thừa} & \textbf{Biến giữ lại liên quan} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Memory\_Bus}
  & Chiều rộng bus (bit) chỉ là một trong hai thừa số của băng thông: \texttt{Memory\_Bandwidth} $= \texttt{Memory\_Bus} \times \texttt{Memory\_Speed}$
  & \texttt{Memory\_Bandwidth} \\
\hline
\texttt{Pixel\_Rate}
  & $= \texttt{ROPs} \times \texttt{Core\_Speed}$ --- tốc độ lấp đầy pixel, dẫn xuất trực tiếp.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Texture\_Rate}
  & $= \texttt{TMUs} \times \texttt{Core\_Speed}$ --- tốc độ ánh xạ texture, dẫn xuất trực tiếp.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Shader}, \texttt{TMUs}, \texttt{ROPs}
  & Số đơn vị tính toán tăng theo xung nhịp và thế hệ kiến trúc; tương quan cấu trúc cao với \texttt{Core\_Speed}.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Memory\_Speed}
  & Tốc độ xung nhịp VRAM; cùng với \texttt{Memory\_Bus} xác định băng thông.
  & \texttt{Memory\_Bandwidth} \\
\hline
\texttt{Process}
  & Kích thước tiến trình litho (nm) giảm đơn điệu theo năm phát hành; tác động đã được nắm bắt bởi \texttt{Release\_Date}.
  & \texttt{Release\_Date} \\
\hline
\texttt{L2\_Cache}
  & Dung lượng cache tỷ lệ với băng thông bộ nhớ và độ phức tạp lõi xử lý.
  & \texttt{Memory\_Bandwidth}, \texttt{Core\_Speed} \\
\hline
\texttt{Boost\_Clock}
  & Xung nhịp tăng tốc tự động; tương quan cực cao với \texttt{Core\_Speed} --- đưa cả hai vào mô hình gây đa cộng tuyến nghiêm trọng.
  & \texttt{Core\_Speed} \\
\hline
\end{longtable}
\normalsize

Sau khi lọc qua Tầng 1 và Tầng 2, ta còn lại các biến:

\renewcommand{\arraystretch}{1.1}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|l|l|p{5.5cm}|l|}
\caption{Danh sách biến sử dụng trong mô hình và vai trò tương ứng}
\label{tab:bien} \\
\hline
\textbf{Biến} & \textbf{Kiểu} & \textbf{Mô tả} & \textbf{Vai trò} \\
\hline
\endfirsthead
\hline
\textbf{Biến} & \textbf{Kiểu} & \textbf{Mô tả} & \textbf{Vai trò} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Manufacturer} & Nominal & Hãng sản xuất (NVIDIA, AMD) & Độc lập (phân loại) \\
\texttt{Memory\_Type} & Nominal & Chuẩn VRAM (GDDR5, GDDR3, DDR3, HBM\ldots) & Độc lập (phân loại) \\
\texttt{Memory\_Bandwidth} & Liên tục & Băng thông bộ nhớ (GB/s) & Độc lập (số) \\
\texttt{Memory} & Rời rạc & Dung lượng VRAM (GB/MB) & Độc lập (số) \\
\texttt{Max\_Power} & Liên tục & Công suất tiêu thụ (Watt) & Độc lập (số) \\
\texttt{Core\_Speed} & Liên tục & Tốc độ xung nhịp lõi (MHz) & Độc lập (số) \\
\texttt{Release\_Year} & Số nguyên & Năm phát hành (parse và chuyển đổi từ Release\_Date) & Độc lập (số) \\
\texttt{Release\_Price} & Liên tục & Giá phát hành (USD) & Phụ thuộc $Y$ \\
\hline
\end{longtable}
\normalsize

\subsection{Loại bỏ giá trị ngoại lai (Outliers)}

Nhóm sử dụng phương pháp IQR (Tứ phân vị) để phát hiện và loại bỏ ngoại lai dựa trên định nghĩa: $Q_1 - 1{,}5 \times \mathrm{IQR}$ và $Q_3 + 1{,}5 \times \mathrm{IQR}$.

Một nguyên tắc dễ bị vi phạm: \textbf{ranh giới IQR phải được tính trên tập dữ liệu gốc} trước khi điền khuyết. Nếu điền trung vị cho hàng trăm ô trống trước, khoảng tứ phân vị sẽ co lại giả tạo, dẫn đến nhiều quan sát bình thường bị gán nhầm là ngoại lai và phương pháp Tukey mất đi ý nghĩa.

Do đó, nhóm thực hiện loại bỏ outliers trước khi điền khuyết, đảm bảo rằng ranh giới IQR phản ánh đúng sự phân bố tự nhiên của dữ liệu gốc.

\subsection{Chia tập mẫu thành tập huấn luyện và tập kiểm tra}

Sau khi loại bỏ ngoại lai, tập dữ liệu được chia ngẫu nhiên theo tỷ lệ \textbf{90:10} thành tập huấn luyện (\texttt{train\_data}) và tập kiểm tra (\texttt{test\_data}). Trong R, ta sử dụng hàm \texttt{set.seed(2026252)} để đặt hạt giống cố định, đảm bảo kết quả nhất quán hoàn toàn sau mỗi lần chạy code nhưng vẫn đảm bảo tính ngẫu nhiên trong việc chọn mẫu.

Việc chia tập được thực hiện \emph{trước} bước điền khuyết. Đây là yêu cầu bắt buộc: nếu điền khuyết toàn bộ dữ liệu rồi mới chia, các tham số thống kê (Median, Mode) sẽ được tính trên cả tập kiểm tra, khiến thông tin của tập kiểm tra rò rỉ vào quá trình xây dựng mô hình --- vi phạm nguyên tắc đánh giá ngoài mẫu.

\subsection{Chiến lược điền khuyết theo vai trò biến}

Việc xử lý các giá trị khuyết, không có phương pháp điền khuyết ``một cho tất cả''. Lựa chọn phương pháp phụ thuộc vào vai trò của biến trong mô hình và đặc điểm phân phối của nó:

\renewcommand{\arraystretch}{1.2}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{4.5cm}|p{3.2cm}|p{6.5cm}|}
\caption{Chiến lược điền khuyết dựa trên vai trò và đặc điểm của biến}
\label{tab:imputation_strategy} \\
\hline
\textbf{Biến} & \textbf{Phương pháp} & \textbf{Cơ sở lý luận} \\
\hline
\endfirsthead
\hline
\textbf{Biến} & \textbf{Phương pháp} & \textbf{Cơ sở lý luận} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Memory} \newline \textit{(bước 1 --- ưu tiên)} & Trích xuất từ tên GPU (Regex) & Tên sản phẩm thường ghi rõ dung lượng VRAM (ví dụ: \textit{``8 GB''}); regex \texttt{\textbackslash b(\textbackslash d+)\textbackslash s*(GB|MB)\textbackslash b} khôi phục giá trị gốc chính xác hơn bất kỳ phép nội suy thống kê nào. \\
\hline
\texttt{Memory} \newline \textit{(bước 2 --- dự phòng)} & Yếu vị (Mode) & Áp dụng khi tên GPU không chứa thông tin VRAM; đảm bảo giá trị điền khuyết luôn là một chuẩn phần cứng có thật (rời rạc). \\
\hline
\texttt{Core\_Speed}, \texttt{Max\_Power}, \texttt{Memory\_Bandwidth}, \texttt{Release\_Year} & Trung vị (Median) & Bền vững với các giá trị cực đại (flagship GPU) làm lệch trung bình. \\
\hline
\texttt{Memory\_Type}, \texttt{Manufacturer} & Yếu vị (Mode) & Bảo toàn cấu trúc và tần suất của các nhóm định danh. \\
\hline
\end{longtable}
\normalsize

\noindent\textbf{Lưu ý chống rò rỉ dữ liệu (Data Leakage):} Toàn bộ tham số điền khuyết (Median, Mode) được tính toán \emph{chỉ} từ tập huấn luyện (\texttt{train\_data}), sau đó áp dụng sang tập kiểm tra (\texttt{test\_data}). Điều này ngăn thông tin của tập kiểm tra ảnh hưởng ngược lên quá trình xây dựng mô hình.

\subsection{Chuẩn hóa và chuyển đổi biến}

Các biến độc lập trải qua ba bước biến đổi, áp dụng ở các giai đoạn khác nhau trong pipeline (Bảng~\ref{tab:var_encoding}):

\renewcommand{\arraystretch}{1.3}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{4.8cm}|p{2.0cm}|p{7.4cm}|}
\caption{Tóm tắt biến đổi và mã hóa biến theo pipeline thực tế}
\label{tab:var_encoding} \\
\hline
\textbf{Biến} & \textbf{Loại} & \textbf{Xử lý} \\
\hline
\endfirsthead
\hline
\textbf{Biến} & \textbf{Loại} & \textbf{Xử lý} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Memory\_Bandwidth} & Liên tục & Log-transform: \texttt{log\_Memory\_Bandwidth = log(Memory\_Bandwidth)}, dùng trong mô hình log-linear và mô hình chuẩn hóa. \\
\hline
\texttt{Max\_Power}, \texttt{Memory}, \texttt{Core\_Speed}, \texttt{Release\_Year}, \texttt{log\_Memory\_Bandwidth} & Số & Z-score $z = (x - \bar{x})/s$, tham số tính từ \texttt{train\_data}. \textbf{Chỉ} áp dụng cho mô hình chuẩn hóa (phân tích tầm quan trọng biến); mô hình dự báo chính dùng giá trị thô. \\
\hline
\end{longtable}
\normalsize

\textbf{Log-transform \texttt{Memory\_Bandwidth}.} Băng thông bộ nhớ có phân phối lệch phải mạnh do các flagship GPU tạo đuôi dài. Logarithm tự nhiên nén đuôi phân phối, ổn định phương sai phần dư và tuyến tính hóa quan hệ với log-giá trong mô hình log-linear.

\textbf{Z-score chỉ phục vụ phân tích tầm quan trọng biến.} Mô hình dự báo giá chính dùng giá trị thô (ngoại trừ \texttt{log\_Memory\_Bandwidth}). Mô hình chuẩn hóa áp dụng thêm Z-score trên năm biến số, đưa tất cả về cùng đơn vị ``độ lệch chuẩn'' để hệ số $\hat{\beta}$ có thể so sánh trực tiếp theo độ lớn.

\subsection{Xây dựng mô hình thống kê}

Sau khi dữ liệu đã được làm sạch và chuẩn hóa, bước tiếp theo là xây dựng các mô hình thống kê để trả lời hai câu hỏi nghiên cứu chính.

\subsubsection{Xây dựng và lựa chọn mô hình Hồi quy Tuyến tính Bội}

Quy trình xây dựng mô hình dự báo giá GPU được thực hiện thông qua việc so sánh hai mô hình: mô hình hồi quy tuyến tính bội cơ bản (MLR) và mô hình hồi quy tuyến tính dạng Log-Linear.

\textbf{Mô hình Hồi quy Tuyến tính Bội cơ bản (Linear).} Mô hình được xây dựng với toàn bộ các biến độc lập và biến phụ thuộc là giá gốc $Y$:
\[
  Y = \beta_0 + \beta_1 X_1 + \cdots + \beta_k X_k + \varepsilon
\]

\textbf{Mô hình Log-Linear.} Đặc thù định giá sản phẩm công nghệ là các yếu tố kỹ thuật thường tác động lên giá theo \textbf{tỷ lệ phần trăm} (multiplicative) thay vì cộng gộp tuyệt đối. Do đó, nhóm áp dụng \textbf{biến đổi logarit tự nhiên} lên biến phụ thuộc để tạo thành mô hình Log-Linear:
\[
  \log(Y) = \beta_0 + \beta_1 X_1 + \cdots + \beta_k X_k + \varepsilon
\]

Phép biến đổi này nén đuôi phải của phân phối giá (do các card flagship tạo ra), đưa phân phối về dạng đối xứng hơn và ổn định phương sai phần dư. Đặc biệt, biến \texttt{Memory\_Bandwidth} cũng được biến đổi logarit (\texttt{log\_Memory\_Bandwidth}) để tuyến tính hóa quan hệ với log-giá.

\textbf{Kiểm định đa cộng tuyến (VIF):} Sau khi xây dựng, cả hai mô hình đều được kiểm tra hiện tượng đa cộng tuyến. Hệ số phóng đại phương sai (VIF) được tính toán và ta sẽ loại những biến có $\text{VIF} < 10$ rồi xây dựng lại mô hình (nếu tồn tại biến gây nhiễu).

\subsubsection{Các kiểm định thống kê và chẩn đoán mô hình}

Sau khi xây dựng, mô hình Log-Linear được kiểm định các giả định quan trọng của hồi quy tuyến tính:
\begin{itemize}
    \item \textbf{Kiểm định Breusch-Pagan:} Đánh giá hiện tượng phương sai sai số thay đổi (Heteroscedasticity).
    \item \textbf{Kiểm định Shapiro-Wilk:} Kiểm tra phân phối chuẩn của phần dư, đảm bảo tính hợp lệ cho các suy diễn thống kê.
\end{itemize}

Bên cạnh đó, để trả lời câu hỏi về chênh lệch định giá thương hiệu có thực sự mang ý nghĩa thống kê hay không, bài toán đặt ra giả thuyết:
\begin{itemize}
    \item $H_0$: Không có sự khác biệt về giá trung bình giữa NVIDIA và AMD ($\mu_{\mathrm{NVIDIA}} = \mu_{\mathrm{AMD}}$).
    \item $H_1$: Có sự khác biệt về giá trung bình giữa hai hãng ($\mu_{\mathrm{NVIDIA}} \neq \mu_{\mathrm{AMD}}$).
\end{itemize}

\textbf{Welch's ANOVA} được áp dụng để kiểm tra sự khác biệt về giá phát hành trung bình giữa hai hãng NVIDIA và AMD. Do chiến lược phân khúc khác nhau, phương sai giá của hai hãng không đồng nhất, do đó Welch's ANOVA là lựa chọn phù hợp nhất vì phương pháp này tự động hiệu chỉnh bậc tự do mà không yêu cầu giả định đồng nhất phương sai.

\subsubsection{Đánh giá mô hình và phân tích tầm quan trọng của biến}

Mô hình được đánh giá khả năng dự báo trên tập kiểm tra (Test Set) thông qua các chỉ số RMSE (Root Mean Squared Error), MAE (Mean Absolute Error) và $R^2$ ngoài mẫu (Out-of-sample $R^2$).

Cuối cùng, để so sánh mức độ tác động của từng thông số phần cứng lên giá, dữ liệu được chuẩn hóa về cùng thang đo (Z-score). Mô hình Log-Linear được huấn luyện lại trên tập dữ liệu chuẩn hóa, qua đó các hệ số hồi quy $\hat{\beta}$ có thể được so sánh trực tiếp, chỉ ra thông số nào mang tính quyết định nhất đến việc định giá GPU. Kết quả của mô hình cũng được kiểm chứng thêm qua một tập dữ liệu GPU đời mới mô phỏng thực tế.

\subsection{Sơ đồ tổng quan quy trình phân tích}

Toàn bộ lộ trình --- từ tập dữ liệu thô đến mô hình log-linear cuối cùng --- được tóm tắt trong sơ đồ khối dưới đây:

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
    node distance=0.8cm and 1.5cm,
    box/.style={rectangle, draw=black!80, fill=white, text centered, text width=5.5cm, minimum height=1.1cm, font=\small},
    diamond_box/.style={diamond, draw=black!80, fill=white, text centered, text width=2.4cm, inner sep=0pt, font=\scriptsize, aspect=1.8},
    bg_box/.style={rectangle, draw=black!60, fill=gray!10, inner sep=18pt, rounded corners=3mm},
    >=stealth,
    thick
]

% --- KHỐI 1 (Cột trái) ---
\node[box] (b1) {Bước 1: Tải dữ liệu \&\\ Chọn lọc 8 biến (N=3.406)};
\node[box, below=of b1] (b2) {Bước 2: Làm sạch, xóa khuyết giá};
\node[box, below=of b2] (b3) {Bước 3: Xử lý ngoại lai (IQR)};
\node[box, below=of b3] (b4) {Bước 4: Chia tập dữ liệu\\ Train/Test (90:10)};
\node[box, below=of b4] (b5) {Bước 5: Điền khuyết trên Train\\ (Regex, Mode, Median)};
\node[box, below=of b5] (b6) {Bước 6: Biến đổi Logarit\\ \texttt{Memory\_Bandwidth}};

% --- KHỐI 2 (Cột phải) ---
\node[box, right=2.8cm of b1] (b7) {Bước 7: Xây dựng \& so sánh\\ MLR cơ bản vs Log-Linear};
\node[diamond_box, below=0.5cm of b7] (d1) {Đạt chuẩn\\VIF $< 10$?};
\node[rectangle, draw=black!80, fill=white, text centered, text width=2.5cm, minimum height=0.8cm, font=\scriptsize, right=1.2cm of d1] (drop1) {Loại biến\\gây nhiễu};
\node[box, below=0.5cm of d1] (b8) {Bước 8: Kiểm định thống kê\\ (ANOVA, BP, Shapiro-Wilk)};
\node[box, below=of b8] (b9) {Bước 9: Đánh giá mô hình\\ trên tập Test ngoài mẫu};
\node[box, below=of b9] (b10) {Bước 10: Chuẩn hóa Z-Score\\ \& Phân tích Feature Importance};
\node[box, below=of b10] (b11) {Bước 11: Dự báo giá GPU mới\\ (Validation in-the-wild)};
\node[box, below=of b11] (b12) {Kết luận \& Trực quan hóa};

% Backgrounds
\begin{scope}[on background layer]
    \node[bg_box, fit=(b1) (b6)] (bg1) {};
    \node[bg_box, fit=(b7) (drop1) (d1) (b12)] (bg2) {};
\end{scope}

% Block Labels
\node[font=\bfseries\small, text width=5cm, align=center, above=0.2cm of bg1.north] (kh1_label) {Khối 1: Tiền xử lý dữ liệu};
\node[font=\bfseries\small, text width=6cm, align=center, above=0.2cm of bg2.north] (kh2_label) {Khối 2: Xây dựng \& Đánh giá mô hình};

% Arrows Khối 1
\draw[->] (b1) -- (b2);
\draw[->] (b2) -- (b3);
\draw[->] (b3) -- (b4);
\draw[->] (b4) -- (b5);
\draw[->] (b5) -- (b6);

% Link Khối 1 -> Khối 2
\draw[rounded corners=8pt, ->] (b6.east) -- ++(1.4,0) |- (b7.west);

% Arrows Khối 2
\draw[->] (b7) -- (d1);
\draw[->] (d1) -- node[midway, above, font=\scriptsize] {Không} (drop1);
\draw[rounded corners=8pt, ->] (drop1.north) |- (b7.east); 
\draw[->] (d1) -- node[midway, left, font=\scriptsize] {Có} (b8);
\draw[->] (b8) -- (b9);
\draw[->] (b9) -- (b10);
\draw[->] (b10) -- (b11);
\draw[->] (b11) -- (b12);

\end{tikzpicture}
}
\caption{Sơ đồ quy trình xử lý và phân tích dữ liệu cập nhật.}
\label{fig:quy_trinh}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%