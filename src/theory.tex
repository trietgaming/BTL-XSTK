% !TEX root = main.tex
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Cơ sở Lý thuyết}
\label{sec:co_so_ly_thuyet}

\subsection{Định giá GPU: Hiệu năng, thương hiệu và thời gian}

Card đồ họa (GPU) là một trong số ít linh kiện điện tử mà giá bán và thông số kỹ thuật thường không tỷ lệ tuyến tính với nhau. Một GPU flagship của NVIDIA có thể đắt gấp đôi một card AMD cùng thế hệ dù chênh lệch hiệu năng không tương xứng --- phần còn lại là \textit{định giá thương hiệu}, \textit{chiến lược phân khúc} và \textit{hệ sinh thái phần mềm}. Điều đó đặt ra câu hỏi không đơn giản: \textbf{giá phát hành GPU thực sự được cấu thành bởi những yếu tố nào?}

Đề tài tiếp cận câu hỏi này qua hai góc nhìn bổ trợ:

\begin{itemize}
  \item \textbf{Hồi quy đa biến}: Dự đoán giá và định lượng tác động đồng thời của TDP, xung nhịp, dung lượng bộ nhớ, năm phát hành và thương hiệu lên giá. Mô hình cho phép trả lời: nếu tăng VRAM thêm 1 độ lệch chuẩn trong khi giữ nguyên mọi thông số khác, giá GPU thay đổi bao nhiêu phần trăm?
  \item \textbf{Kiểm định ANOVA}: Kiểm tra riêng biệt xem khoảng cách giá NVIDIA--AMD có ý nghĩa thống kê, trước khi đưa biến thương hiệu vào mô hình đa biến như một biến giả.
\end{itemize}

Thách thức nằm ở chỗ phần lớn GPU trong bộ dữ liệu không có giá niêm yết chính thức ($\approx 84\%$ thiếu giá), và 34 thuộc tính ban đầu chứa nhiều thông tin dư thừa. Các phần tiếp theo mô tả phương pháp luận để giải quyết những thách thức đó.

\subsection{Bản chất dữ liệu} \label{sec:data_characteristics}

Dữ liệu phần cứng GPU mang một số đặc thù quan trọng cần lưu ý trước khi tiến hành xử lý và xây dựng mô hình:

\begin{itemize}
  \item \textbf{Đặc tính rời rạc của bộ nhớ:} Các biến số như băng thông bộ nhớ (\texttt{Memory\_Bus}) và dung lượng bộ nhớ (\texttt{Memory}) mang bản chất là các biến \textbf{rời rạc}. Chúng chỉ tồn tại theo các chuẩn phần cứng nhất định (ví dụ: bus 64/128/256/512 bit; dung lượng 2/4/8/16~GB).
  \item \textbf{Sự phân mảnh nhãn thương hiệu (ATI và AMD):} ATI Technologies là tên cũ của bộ phận GPU của AMD trước khi bị thâu tóm vào năm 2006. Hiện tại, bộ dữ liệu thô tồn tại song song hai nhãn \texttt{"ATI"} và \texttt{"AMD"} cho cùng một dòng sản phẩm. Hai nhãn này thực chất cần được gộp chung thành một thực thể thương hiệu duy nhất.
  \item \textbf{Sự khuyết thiếu dữ liệu giá của Intel:} Toàn bộ các GPU của hãng Intel trong bộ dữ liệu thô đều thiếu thông tin về biến phụ thuộc là giá phát hành (\texttt{Release\_Price}), do đó các sản phẩm này không thể được sử dụng trong việc xây dựng mô hình định giá và cần loại bỏ hoàn toàn.
  \item \textbf{Các sản phẩm có cấu trúc định giá dị biệt:} Dữ liệu chứa một số nhóm sản phẩm có cơ chế định giá nằm ngoài quy luật phần cứng thông thường và không phụ thuộc vào các thuộc tính có trong bộ dữ liệu. Điển hình như: GPU Workstation (Quadro/FirePro) định giá theo chứng chỉ phần mềm; các cấu hình kép (Crossfire/Dual Core) thực chất là nhiều GPU bị cộng dồn thông số nhưng bán chung với nhau; các bản độ tản nhiệt nước cao cấp (Hydro Copper); hoặc sản phẩm tin đồn chưa từng ra mắt (not released). Nếu không được thanh lọc cẩn thận, nhóm dữ liệu dị biệt này sẽ gây nhiễu nặng nề cho phương trình dự báo của thị trường Consumer.
\end{itemize}

\subsection{Thu hẹp không gian đặc trưng}

Đưa toàn bộ 34 thuộc tính vào mô hình không những không cải thiện khả năng dự đoán mà còn làm giảm chất lượng: biến trùng lặp gây đa cộng tuyến, biến không liên quan thêm nhiễu và tăng phương sai ước lượng. Nhóm giải quyết vấn đề này bằng quy trình sàng lọc \textbf{ba tầng khách quan} --- mỗi tầng nhắm đến một nguồn nhiễu hoàn toàn khác nhau:

\begin{figure}[H]
\centering
\resizebox{0.72\textwidth}{!}{
\begin{tikzpicture}[
  tier/.style={rectangle, draw=black!65, fill=#1, rounded corners=4pt,
               text centered, minimum width=8.5cm, minimum height=1.15cm, font=\small},
  note/.style={rectangle, draw=gray!50, fill=gray!5, rounded corners=2pt,
               font=\scriptsize\itshape, text width=3.8cm, align=left,
               inner sep=4pt},
  >=stealth, thick
]
  \node[tier=gray!12] (t0) {Không gian đặc trưng ban đầu: \textbf{34 thuộc tính}};

  \node[tier=yellow!18, below=0.7cm of t0] (t1)
    {\textbf{Tầng 1:} Loại biến không liên quan kinh tế đến giá};
  \node[note, right=0.6cm of t1] {Định danh sản phẩm,\\API phần mềm,\\cờ nhị phân mất cân bằng};

  \node[tier=orange!12, below=0.7cm of t1] (t2)
    {\textbf{Tầng 2:} Loại biến dẫn xuất hoặc thông tin trùng lặp};
  \node[note, right=0.6cm of t2] {Nguy cơ đa cộng\\tuyến (VIF cao)};

  \node[tier=red!10, below=0.7cm of t2] (t3)
    {\textbf{Tầng 3:} Loại biến thiếu dữ liệu nghiêm trọng ($> 30\%$)};
  \node[note, right=0.6cm of t3] {Xác định thực nghiệm\\qua phân tích\\tỷ lệ khuyết (Phần~\ref{sec:code_xu_ly_du_lieu})};

  \node[tier=green!15, below=0.7cm of t3] (t4)
    {Tập đặc trưng chính thức: \textbf{8 biến}};

  \draw[->] (t0) -- (t1);
  \draw[->] (t1) -- (t2);
  \draw[->] (t2) -- (t3);
  \draw[->] (t3) -- (t4);
\end{tikzpicture}
}
\caption{Chiến lược lọc ba tầng: thu hẹp không gian đặc trưng từ 34 xuống 8 biến cốt lõi.}
\label{fig:filter_strategy}
\end{figure}

Tầng~1 và Tầng~2 có thể xác định hoàn toàn bằng lý luận trước khi nhìn vào dữ liệu. Tầng~3 cần đo tỷ lệ khuyết thực tế và được giải quyết trong Phần~\ref{sec:buoc_1_tai_du_lieu}.

\renewcommand{\arraystretch}{1.1}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{5.2cm}|p{3.5cm}|p{5.5cm}|}
\caption{Tầng 1 --- 14 biến bị loại do không có ý nghĩa kinh tế}
\label{tab:tier1_vars} \\
\hline
\textbf{Biến bị loại} & \textbf{Nội dung} & \textbf{Lý do cụ thể} \\
\hline
\endfirsthead
\hline
\textbf{Biến bị loại} & \textbf{Nội dung} & \textbf{Lý do cụ thể} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Name}, \texttt{Architecture}
  & Nhãn định danh sản phẩm
  & Không phải đặc tính kỹ thuật định lượng, không có cơ chế trực tiếp tác động đến chi phí sản xuất. \\
\hline
\texttt{DVI\_Connection}, \texttt{HDMI\_Connection}, \texttt{VGA\_Connection}
  & Đặc tả cổng đầu ra hiển thị
  & Số lượng và loại cổng phụ thuộc vào phân khúc sản phẩm, không phản ánh hiệu năng GPU. \\
\hline
\texttt{Direct\_X}, \texttt{Open\_GL}
  & Phiên bản API đồ họa
  & Hoàn toàn phụ thuộc vào driver phần mềm và thế hệ kiến trúc, không phản ánh sức mạnh phần cứng thực. \\
\hline
\texttt{Best\_Resolution}, \texttt{Resolution\_WxH}, \texttt{Power\_Connector}
  & Thông số hiển thị và loại đầu nối nguồn
  & Dẫn xuất gián tiếp từ cấu hình phần cứng, không mang thêm thông tin độc lập về giá trị GPU. \\
\hline
\texttt{Dedicated}, \texttt{Integrated}, \texttt{Notebook\_GPU}, \texttt{SLI\_Crossfire}
  & Số nhị phân mất cân bằng nặng
  & Gần như toàn bộ mẫu thuộc cùng một nhãn (ví dụ: $>$98\% là GPU dedicated), biến không mang thông tin phân biệt. \\
\hline
\end{longtable}
\normalsize

\renewcommand{\arraystretch}{1.1}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{3.5cm}|p{6.8cm}|p{3.2cm}|}
\caption{Tầng 2 --- 9 biến bị loại do thông tin dư thừa (nguy cơ đa cộng tuyến)}
\label{tab:tier2_vars} \\
\hline
\textbf{Biến bị loại} & \textbf{Quan hệ dư thừa} & \textbf{Biến giữ lại liên quan} \\
\hline
\endfirsthead
\hline
\textbf{Biến bị loại} & \textbf{Quan hệ dư thừa} & \textbf{Biến giữ lại liên quan} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Memory\_Bandwidth}
  & $= \texttt{Memory\_Bus} \times \texttt{Memory\_Speed}$ --- hoàn toàn xác định bởi hai biến kia.
  & \texttt{Memory\_Bus} \\
\hline
\texttt{Pixel\_Rate}
  & $= \texttt{ROPs} \times \texttt{Core\_Speed}$ --- tốc độ lấp đầy pixel, dẫn xuất trực tiếp.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Texture\_Rate}
  & $= \texttt{TMUs} \times \texttt{Core\_Speed}$ --- tốc độ ánh xạ texture, dẫn xuất trực tiếp.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Shader}, \texttt{TMUs}, \texttt{ROPs}
  & Số đơn vị tính toán tăng theo xung nhịp và thế hệ kiến trúc; tương quan cấu trúc cao với \texttt{Core\_Speed}.
  & \texttt{Core\_Speed} \\
\hline
\texttt{Memory\_Speed}
  & Tốc độ xung nhịp VRAM; cùng với \texttt{Memory\_Bus} xác định băng thông --- đưa cả hai vào mô hình gây đa cộng tuyến.
  & \texttt{Memory\_Bus} \\
\hline
\texttt{Process}
  & Kích thước tiến trình litho (nm) giảm đơn điệu theo năm phát hành; tác động đã được nắm bắt bởi \texttt{Release\_Date}.
  & \texttt{Release\_Date} \\
\hline
\texttt{L2\_Cache}
  & Dung lượng cache tỷ lệ với băng thông bộ nhớ và độ phức tạp lõi xử lý.
  & \texttt{Memory\_Bus}, \texttt{Core\_Speed} \\
\hline
\end{longtable}
\normalsize

\subsection{Tiền xử lý dữ liệu}

\subsubsection{Lọc và làm sạch dữ liệu thô}

Trước khi xử lý ngoại lai hay điền khuyết, bộ dữ liệu thô cần trải qua một chuỗi thao tác làm sạch để đảm bảo tính nhất quán và phù hợp với mục tiêu phân tích:

\begin{enumerate}
  \item Hợp nhất toàn bộ giá trị \texttt{"ATI"} trong cột \texttt{Manufacturer} thành \texttt{"AMD"}.

  \item Loại bỏ các dòng có \texttt{Manufacturer == "Intel"}.

  \item \textbf{Trích xuất số và ép kiểu dữ liệu.} Các trường số trong dữ liệu gốc thường đi kèm đơn vị văn bản (như \texttt{"141 Watts"}, \texttt{"8 GB"}). Nhóm sử dụng biểu thức chính quy (Regular Expression) để trích xuất chính xác phần số, sau đó thực hiện ép kiểu \texttt{as.numeric()}. Quy trình này đảm bảo các giá trị không hợp lệ hoặc chuỗi rỗng được chuyển thành \texttt{NA} thực sự, tạo tiền đề cho các bước xử lý ngoại lai và điền khuyết.
\end{enumerate}

\subsubsection{Loại bỏ giá trị ngoại lai (Outliers)}

Nhóm sử dụng phương pháp IQR (Tứ phân vị) để phát hiện và loại bỏ ngoại lai dựa trên định nghĩa: $Q_1 - 1{,}5 \times \mathrm{IQR}$ và $Q_3 + 1{,}5 \times \mathrm{IQR}$.

Một nguyên tắc dễ bị vi phạm: \textbf{ranh giới IQR phải được tính trên tập dữ liệu gốc} trước khi điền khuyết. Nếu điền trung vị cho hàng trăm ô trống trước, khoảng tứ phân vị sẽ co lại giả tạo, dẫn đến nhiều quan sát bình thường bị gán nhầm là ngoại lai và phương pháp Tukey mất đi ý nghĩa.

Quan sát sơ bộ dữ liệu, GPU Quadro giá \$14.999 hay card workstation công suất 500W không phải sai số nhập liệu mà chúng tồn tại thực tế, nhưng thuộc \textbf{quần thể hoàn toàn khác}: phân khúc Workstation/Enterprise (Quadro, Tesla) được định giá theo chứng chỉ phần mềm ISV, bộ nhớ ECC và hỗ trợ doanh nghiệp B2B (Đã được đề cập ở Phần~\ref{sec:data_characteristics}). Nếu giữ lại, mô hình bị ép giải thích khoảng giá \$23--\$14.999 chỉ từ thông số phần cứng không đủ, gây vi phạm giả định OLS và sai lệch dự đoán cho phân khúc người dùng phổ thông. Do đó, nhóm quyết định \textbf{loại bỏ} các quan sát vượt ngưỡng Tukey ($> Q_3 + 1{,}5 \times \mathrm{IQR}$) trên biến \texttt{Release\_Price}.

\subsubsection{Chiến lược điền khuyết theo vai trò biến}

Việc xử lý các giá trị khuyết, không có phương pháp điền khuyết ``một cho tất cả''. Lựa chọn phương pháp phụ thuộc vào vai trò của biến trong mô hình và đặc điểm phân phối của nó:

\renewcommand{\arraystretch}{1.2}
\small
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|p{4.5cm}|p{3.2cm}|p{6.5cm}|}
\caption{Chiến lược điền khuyết dựa trên vai trò và đặc điểm của biến}
\label{tab:imputation_strategy} \\
\hline
\textbf{Đặc điểm biến} & \textbf{Phương pháp} & \textbf{Cơ sở lý luận} \\
\hline
\endfirsthead
\hline
\textbf{Đặc điểm biến} & \textbf{Phương pháp} & \textbf{Cơ sở lý luận} \\
\hline
\endhead
\hline
\endlastfoot
\textbf{Biến phụ thuộc $Y$} \newline (\texttt{Release\_Price}) & Xóa dòng & Tránh vòng lặp logic (mô hình tự học từ dữ liệu giả do chính nó tạo ra). \\
\textbf{Biến số liên tục} \newline (\texttt{Max\_Power}, \texttt{Core\_Speed}) & Trung vị (Median) & Bền vững với các giá trị cực đại (flagship GPU) làm lệch trung bình. \\
\textbf{Biến số rời rạc} \newline (\texttt{Memory}, \texttt{Memory\_Bus}) & Yếu vị (Mode) & Đảm bảo giá trị điền khuyết luôn là một chuẩn phần cứng có thật. \\
\textbf{Biến phân loại} \newline (\texttt{Manufacturer}, \texttt{Memory\_Type}) & Yếu vị (Mode) & Bảo toàn cấu trúc và tần suất của các nhóm định danh. \\
\hline
\end{longtable}
\normalsize

\subsubsection{Chuẩn hóa và mã hóa biến}

Bảy biến độc lập được xử lý theo ba nhóm (Bảng~\ref{tab:var_encoding}):

\renewcommand{\arraystretch}{1.3}
\setlength\LTleft{\fill}
\setlength\LTright{\fill}
\begin{longtable}{|l|l|l|}
\caption{Tóm tắt xử lý chuẩn hóa và mã hóa biến}
\label{tab:var_encoding} \\
\hline
\textbf{Biến} & \textbf{Loại} & \textbf{Xử lý} \\
\hline
\endfirsthead
\hline
\textbf{Biến} & \textbf{Loại} & \textbf{Xử lý} \\
\hline
\endhead
\hline
\endlastfoot
\texttt{Max\_Power}, \texttt{Core\_Speed}     & Liên tục  & Z-score \\
\texttt{Memory\_Bus}, \texttt{Memory}         & Rời rạc   & Z-score \\
\texttt{Release\_Date}                        & Rời rạc  & Trích xuất năm (giữ nguyên số) \\
\texttt{Manufacturer}, \texttt{Memory\_Type}  & Phân loại & \texttt{factor()} \\
\hline
\end{longtable}

\textbf{Bốn biến số} có thang đo hoàn toàn khác nhau (Watts, MHz, bit, GB) nên được chuẩn hóa Z-score $z_i = (x_i - \bar{x})/s$ về cùng đơn vị ``độ lệch chuẩn'', cho phép so sánh trực tiếp độ lớn hệ số $\hat{\beta}$ trong mô hình log-linear. Việc xử lý Z-score cho \texttt{Memory\_Bus} và \texttt{Memory} vẫn hợp lệ do tính chất thứ tự có nhiều mức phân biệt của chúng, đảm bảo quan hệ tuyến tính vẫn xấp xỉ tốt trong hồi quy.

\texttt{Release\_Date} lưu dạng chuỗi ngày tháng (\texttt{"01-Mar-2009"}) không thể đưa trực tiếp vào hồi quy. Nhóm trích xuất phần năm thành số nguyên (2000--2023) vì chỉ thông tin \textit{năm} mới mang tín hiệu kinh tế có ý nghĩa: chu kỳ thế hệ GPU là 1--2 năm, trong khi ngày và tháng không phản ánh bất kỳ đặc trưng định giá nào.

% Về bản chất, \texttt{release\_year} là biến \textbf{rời rạc} (chỉ nhận giá trị nguyên). Tuy nhiên, khác với \texttt{Memory\_Bus} hay \texttt{Memory} có khoảng cách không đều giữa các chuẩn phần cứng, các năm liên tiếp cách đều đúng 1 đơn vị và không có khoảng trống --- đây là điều kiện đủ để hồi quy tuyến tính xấp xỉ tốt quan hệ xu hướng theo thời gian.

Hai biến phân loại được ép kiểu \texttt{factor()} để R sinh biến giả tự động trong \texttt{lm()}. Vì R sắp xếp levels theo alphabet, AMD trở thành nhóm tham chiếu (baseline); hệ số \texttt{Manufacturer NVIDIA} do đó đọc được là ``chênh lệch log-giá giữa NVIDIA và AMD khi kiểm soát các đặc tính kỹ thuật''.

\subsubsection{Khám phá tương quan biến số (EDA)}

Trước khi tiến hành xây dựng mô hình hồi quy chính thức, hệ số tương quan Pearson được phân tích như một bước Khám phá Dữ liệu (Exploratory Data Analysis). Thao tác này nhằm hai mục đích: (1) đánh giá sơ bộ chiều hướng và cường độ tác động của từng đặc tính phần cứng lên giá, và (2) nhận diện sớm các cặp biến độc lập có độ tương quan quá cao. Sự xuất hiện của các cặp biến này sẽ là tín hiệu quan trọng làm tiền đề cho bước kiểm định đa cộng tuyến chuyên sâu ở giai đoạn sau.

\subsection{Xây dựng mô hình thống kê}

Sau khi dữ liệu đã được làm sạch và chuẩn hóa, bước tiếp theo là xây dựng các mô hình thống kê để trả lời hai câu hỏi nghiên cứu chính.

\subsubsection{Xây dựng mô hình Hồi quy Tuyến tính Bội (MLR)}

Quy trình xây dựng mô hình gồm ba giai đoạn tuần tự, mỗi giai đoạn giải quyết một vấn đề riêng biệt trước khi chuyển sang bước tiếp theo.

\textbf{Giai đoạn 1 --- Kiểm định đa cộng tuyến (VIF).} Mô hình hồi quy đầy đủ với toàn bộ $n$ biến độc lập và biến phụ thuộc là giá gốc $Y$ được xây dựng trước:
\[
  Y = \beta_0 + \beta_1 X_1 + \cdots + \beta_n X_n + \varepsilon
\]
Hệ số phóng đại phương sai (VIF) được tính cho từng biến. Bất kỳ biến nào có $\text{VIF} > 5$ (ngưỡng an toàn đa cộng tuyến) sẽ bị loại và mô hình được xây lại với các biến còn lại cho đến khi toàn bộ đều đạt chuẩn.

\textbf{Giai đoạn 2 --- Lọc ngoại lai phần dư.} Mô hình cơ sở sau giai đoạn 1 được dùng để tính phần dư chuẩn hóa cho từng quan sát. Các quan sát có $|\text{std.residual}| > 2{,}5$ được xem xét loại bỏ nếu kết hợp với kiến thức ngành xác nhận chúng là \textbf{ngoại lai cấu trúc} --- tức những sản phẩm có cơ chế định giá mà mô hình không thể giải thích. Bộ dữ liệu sau bước này là tập chính thức để xây mô hình final.

\textbf{Giai đoạn 3 --- Chuyển đổi Log-Linear.} Đặc thù định giá sản phẩm công nghệ là các yếu tố kỹ thuật tác động lên giá theo \textbf{tỷ lệ phần trăm} (multiplicative) thay vì cộng gộp tuyệt đối: việc tăng thêm 2~GB VRAM có tác động giá hoàn toàn khác trên một card \$1.000 so với card \$200. Do đó nhóm áp dụng \textbf{biến đổi logarit tự nhiên} lên biến phụ thuộc để tạo thành mô hình Log-Linear:
\[
  \log(Y) = \beta_0 + \beta_1 X_1 + \beta_2 X_2 + \cdots + \beta_k X_k + \varepsilon
\]

Phép biến đổi này nén đuôi phải của phân phối giá (do các card flagship tạo ra), đưa phân phối về dạng đối xứng hơn và ổn định phương sai. Quan trọng hơn, các hệ số $\hat{\beta}_i$ lúc này mang ý nghĩa kinh tế trực tiếp: khi $X_i$ tăng 1 đơn vị (sau chuẩn hóa), giá GPU thay đổi xấp xỉ $(e^{\hat{\beta}_i} - 1) \times 100\%$. Mô hình Log-Linear này được chọn làm mô hình chính thức để rút ra kết luận.

\subsubsection{Phân tích Phương sai Welch's ANOVA}

Để kiểm tra xem khoảng cách giá trung bình giữa GPU của NVIDIA và AMD có thực sự mang ý nghĩa thống kê hay không, bài toán đặt ra giả thuyết:
\begin{itemize}
    \item $H_0$: Không có sự khác biệt về giá trung bình giữa NVIDIA và AMD ($\mu_{\mathrm{NVIDIA}} = \mu_{\mathrm{AMD}}$).
    \item $H_1$: Có sự khác biệt về giá trung bình giữa hai hãng ($\mu_{\mathrm{NVIDIA}} \neq \mu_{\mathrm{AMD}}$).
\end{itemize}

Thông thường, phân tích phương sai ANOVA cổ điển sẽ được sử dụng. Tuy nhiên, chiến lược sản phẩm của hai hãng rất khác biệt: NVIDIA có nhiều dòng sản phẩm rải rác từ phổ thông đến workstation đắt tiền, trong khi AMD tập trung chủ yếu vào phân khúc người tiêu dùng phổ thông. Sự khác biệt này dẫn đến chênh lệch lớn về phương sai giá giữa hai hãng, làm vi phạm giả định đồng nhất phương sai của ANOVA truyền thống.

Do đó, nhóm quyết định sử dụng \textbf{Welch's ANOVA}. Phương pháp này tự động hiệu chỉnh bậc tự do để cho ra kết quả kiểm định chính xác ngay cả khi phương sai không đồng nhất. Vì bài toán hiện tại chỉ so sánh hai nhóm (NVIDIA và AMD), kết quả của Welch's ANOVA trên thực tế tương đương với kiểm định Welch's T-test, do đó không cần thực hiện thêm các bước hậu kiểm (Post-hoc test).

\subsection{Sơ đồ tổng quan quy trình phân tích}

Toàn bộ lộ trình --- từ tập dữ liệu thô đến mô hình log-linear cuối cùng --- được tóm tắt trong sơ đồ khối dưới đây:

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{
\begin{tikzpicture}[
    node distance=0.8cm and 2.2cm,
    box/.style={rectangle, draw=black!80, fill=white, text centered, text width=4.8cm, minimum height=1.1cm, font=\small},
    small_box/.style={rectangle, draw=black!80, fill=white, text centered, text width=3cm, minimum height=0.8cm, font=\scriptsize},
    diamond_box/.style={diamond, draw=black!80, fill=white, text centered, text width=2.2cm, inner sep=0pt, font=\scriptsize, aspect=1.8},
    bg_box/.style={rectangle, draw=black!60, fill=gray!10, inner sep=20pt, rounded corners=3mm},
    >=stealth,
    thick
]

% --- KHỐI 1 (Cột trái) ---
\node[box] (b1) {Bước 1: Tải dữ liệu \&\\Chọn lọc 8 biến (N=3.406)};
\node[box, below=of b1] (b2) {Bước 2: Chọn lọc, đổi tên \&\\Gộp nhãn thương hiệu AMD};
\node[box, below=of b2] (b3) {Bước 3: Nhận diện ngoại lai\\(IQR trên tập gốc)};
\node[box, below=of b3] (b4) {Bước 4: Xóa giá khuyết,\\ loại Outliers (N=523) \&\\ Điền khuyết Mode/Median};
\node[box, below=of b4] (b5) {Bước 5: Mã hóa Factor\\(Biến phân loại)};
\node[box, below=of b5] (b6) {Bước 6: Chuẩn hóa Z-Score\\\& Tương quan Pearson};

% --- KHỐI 2 (Cột phải) ---
\node[box, right=2.8cm of b1] (b71_1) {Bước 7.1: Hồi quy MLR\\(Mô hình cơ sở N=523)};
\node[diamond_box, below=0.6cm of b71_1] (d1) {Đạt chuẩn\\VIF $< 5$?};
\node[small_box, right=1.2cm of d1] (drop1) {Loại biến nhiễu\\\texttt{memory\_type}};
\node[box, below=0.6cm of d1] (b71_test) {Lọc ngoại lai phần dư \&\\Chuyển đổi Log-Linear};
\node[box, below=0.6cm of b71_test] (b71_log) {Mô hình Final (N=514)\\Đạt độ tối ưu $R^2 \approx 83\%$};
\node[box, below=0.6cm of b71_log] (b72) {Bước 7.2: Phân tích\\Welch's ANOVA};
\node[box, below=0.6cm of b72] (b_final) {Chốt mô hình \& Kết luận\\câu hỏi nghiên cứu};

% Backgrounds
\begin{scope}[on background layer]
    \node[bg_box, fit=(b1) (b6)] (bg1) {};
    \node[bg_box, fit=(b71_1) (drop1) (d1) (b71_test) (b71_log) (b72) (b_final)] (bg2) {};
\end{scope}

% Block Labels
\node[font=\bfseries\small, text width=5cm, align=center, above=0.2cm of bg1.north] (kh1_label) {Khối 1: Tiền xử lý dữ liệu};
\node[font=\bfseries\small, text width=6cm, align=center, above=0.2cm of bg2.north] (kh2_label) {Khối 2: Xây dựng \& Đánh giá mô hình};

% Arrows Khối 1
\draw[->] (b1) -- (b2);
\draw[->] (b2) -- (b3);
\draw[->] (b3) -- (b4);
\draw[->] (b4) -- (b5);
\draw[->] (b5) -- (b6);

% Link Khối 1 -> Khối 2
\draw[rounded corners=8pt, ->] (b6.east) -- ++(1.4,0) |- (b71_1.west);

% Arrows Khối 2
\draw[->] (b71_1) -- (d1);
\draw[->] (d1) -- node[midway, above, font=\scriptsize] {Không} (drop1);
\draw[rounded corners=8pt, ->] (drop1.north) |- (b71_1.east); 

\draw[->] (d1) -- node[midway, left, font=\scriptsize] {Có} (b71_test);
\draw[->] (b71_test) -- (b71_log);
\draw[->] (b71_log) -- (b72);
\draw[->] (b72) -- (b_final);

\end{tikzpicture}
}
\caption{Sơ đồ quy trình xử lý và phân tích dữ liệu}
\label{fig:quy_trinh}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%